{
  "name": "BlockedUser",
  "type": "object",
  "properties": {
    "blocker_email": {
      "type": "string",
      "description": "User who did the blocking"
    },
    "blocked_email": {
      "type": "string",
      "description": "User who got blocked"
    }
  },
  "required": [
    "blocker_email",
    "blocked_email"
  ],
  "rls": {
    "read": {
      "data.blocker_email": "{{user.email}}"
    },
    "create": {
      "data.blocker_email": "{{user.email}}"
    },
    "update": {
      "data.blocker_email": "{{user.email}}"
    },
    "delete": {
      "data.blocker_email": "{{user.email}}"
    }
  }
}